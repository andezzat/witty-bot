// Declaring constants of types used by Send API objects.
const TEMPLATE = {
  BUTTON: 'button',
  GENERIC: 'generic',
};

const BUTTON = {
  POSTBACK: 'postback',
  URL: 'web_url',
};

const ATTACHMENT = {
  TEMPLATE: 'template',
};

module.exports = {
  TEMPLATE,
  BUTTON,
  ATTACHMENT,
};
